package eu.ml82.bpmn_layouter.core.properties;

/**
 * 
 * Whether the layouter returns relative or absolute node positions.
 * (Relative means relative to a container like a pool or lane.)
 *
 */

public enum PosType {
		ABSOLUTE,
		RELATIVE
}
